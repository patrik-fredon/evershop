version: '3.8'

services:
  app:
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "3000:3000"
    command: ["sh", "-c", "sleep 30 && npm run dev"]

volumes:
  node_modules:
